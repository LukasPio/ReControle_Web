import { convertImg } from "./js_functions/swal_db_fires.js";

/*
document.addEventListener('DOMContentLoaded', () => {
    const img = document.getElementById('inf');
    if (img) {
        img.addEventListener('change', (event) => {
            const file = event.target.files[0];

            convertImg(file, function(base64Result) {
                console.log('Imagem em base64:', base64Result);
                const imgElement = document.createElement('img');
                imgElement.src = base64Result; // Isso já inclui "data:image/jpeg;base64,..."
                document.body.appendChild(imgElement);
            })
        })
    }

})
*/